services:
  ytap-python:
    build: .
    env_file:
      - .env
    volumes:
      # YouTube cookies to bypass the datacenter-IP bot check.
      # Create ./cookies.txt before `up`; it is gitignored.
      - ./cookies.txt:/app/cookies.txt:ro
    container_name: ytap-python
    restart: always
